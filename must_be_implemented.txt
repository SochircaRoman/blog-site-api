LoginItem:
	-Input background blur validation (error -> pink, succes -> green);
	-Loading screen (when form is submitting);

RegisterItem:
	-Succes submitting message;

-Decomposition;

NavBar:
	-Dropdown profile;

LoginItem:
	-Succes message when user login;

ProfileItem:
	-Create profile;